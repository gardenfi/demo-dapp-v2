/**
 * Adds a '0x' prefix to the given string if it doesn't already have one.
 *
 * @param {string} str - The input string.
 * @return {string} The input string with a '0x' prefix.
 */
export declare const with0x: (str: string) => string;
/**
 * Trims the '0x' prefix from a given string if it exists.
 *
 * @param {string} str - The input string to trim.
 * @return {string} The trimmed string.
 */
export declare const trim0x: (str: string) => string;
