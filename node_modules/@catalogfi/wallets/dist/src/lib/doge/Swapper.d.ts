import { IDogeWallet } from './dogeWallet';
import { AtomicSwapConfig } from '../ASConfig';

export interface ISwapper {
    init(): Promise<string>;
    redeem(secret: string): Promise<string>;
}
export declare class Swapper implements ISwapper {
    private script;
    private scriptAddress;
    private recipientAddress;
    private refundAddress;
    private timelock;
    private amount;
    private wallet;
    constructor(swapConfig: AtomicSwapConfig, wallet: IDogeWallet);
    init(): Promise<string>;
    redeem(secret: string): Promise<string>;
    refund(): Promise<string>;
}
