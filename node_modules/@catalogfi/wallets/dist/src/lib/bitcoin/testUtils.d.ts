import * as bitcoin from 'bitcoinjs-lib';
export declare const getSignerFromPk: (pk: string, network: bitcoin.Network) => Promise<import('ecpair').ECPairInterface>;
export declare const getScript: (network: bitcoin.Network, type: 'p2sh' | 'p2wsh') => {
    script: Buffer;
    address: string;
};
