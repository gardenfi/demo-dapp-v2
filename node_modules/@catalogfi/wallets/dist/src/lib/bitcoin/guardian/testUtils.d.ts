import { IBitcoinProvider } from '../provider.interface';

export declare class regTestUtils {
    static mine(blocks: number, provider: IBitcoinProvider): Promise<void>;
    /**
     * funds the address with 1 BTC
     */
    static fund(address: string, provider: IBitcoinProvider): Promise<void>;
}
