import { networks } from 'bitcoinjs-lib';
import { BitcoinNetwork } from './provider.interface';

export declare function reversify(val: string): Buffer;
export declare function getBitcoinNetwork(network: BitcoinNetwork): networks.Network;
export declare function parseGuardianAPI(network: BitcoinNetwork, api: string | undefined): string;
