const e = (r) => {
  try {
    return JSON.stringify(r);
  } catch {
    throw new Error("Data is not serializable");
  }
}, n = () => Math.floor(Date.now() / 1e3);
export {
  n as currentTimeInSeconds,
  e as stringify
};
