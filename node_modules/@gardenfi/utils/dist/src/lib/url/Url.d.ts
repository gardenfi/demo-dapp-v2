export declare class Url extends URL {
    constructor(endpoint: string, base?: string | Url);
    endpoint(endpoint: string): Url;
    socket(): string;
    addSearchParams(params: Record<string, string>): Url;
}
